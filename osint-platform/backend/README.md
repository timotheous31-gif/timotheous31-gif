# OSINT Platform — backend

FastAPI service, collector framework, correlation engine, privacy filter and
CLI. See the [platform README](../README.md) and [docs/](../docs) for the full
picture.

```bash
make install-backend     # from the repository root
make dev-backend         # http://localhost:8000/docs
make test                # pytest
make lint                # ruff + black + mypy
```
